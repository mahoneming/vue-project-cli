
const getters = {
	// language: state => state.language.language,
	// isEn: state => state.language.isEn,
	// languageMapping: state => state.language.languageMapping,
	// user: state => state.user,
	// namePath: state => state.breadcrumb.namePath,
	// cacheLive: state => state.breadcrumb.cacheLive,
	// memu_service_tiao: state => state.menuDown.memu_service_tiao,
};
export default getters;
